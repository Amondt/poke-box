import { BrandTitlePipe } from './brand-title.pipe';

describe('BrandTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new BrandTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
